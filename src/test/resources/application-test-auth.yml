spring:
  config:
    activate:
      on-profile:
        - test

jwt:
  secret-key: 'DUMMY'
  access-token:
    expirationMinutes: 30
  refresh-token:
    expirationHours: 24

verification:
  email:
    api: /api/v1/members/verify
    # 10분
    timeoutSecond: 300
  oauth2:
    expirationMinutes: 10