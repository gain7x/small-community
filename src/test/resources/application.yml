spring:
  profiles:
    group:
      test:
        - test-auth
        - test-db
        - test-env
        - test-oauth2
        - prod-mail
    active: test
  messages:
    basename: messages,errors