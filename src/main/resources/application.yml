spring:
  profiles:
    group:
      dev:
        - dev-env
        - dev-auth
        - dev-db
    active: dev
  messages:
    basename: messages,errors

management:
  server:
    port: 7070
  endpoints:
    enabled-by-default: false
    jmx:
      exposure:
        exclude: "*"
    web:
      base-path: /smc-mgnt
      exposure:
        include:
          - info
          - health
  endpoint:
    info.enabled: true
    health.enabled: true